import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.foodlink.merchant',
  appName: 'FoodLink Merchant',
  webDir: '../../dist-merchant',
  bundledWebRuntime: false
};

export default config;
