import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.foodlink.rider',
  appName: 'FoodLink Rider',
  webDir: '../../dist-rider',
  bundledWebRuntime: false
};

export default config;
