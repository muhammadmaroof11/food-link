import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.foodlink.diner',
  appName: 'FoodLink Diner',
  webDir: '../../dist-diner',
  bundledWebRuntime: false
};

export default config;
